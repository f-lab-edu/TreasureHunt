pipeline {
    agent any

    stages {
        stage('build-with-gradle') {
            steps {
                bat "chmod +x gradlew"
                bat "./gradlew clean assembleDebug" // 일반 APK 빌드
                // sh "./gradlew clean app:assembleDebug" // DEBUG APK 빌드
                // sh "./gradlew clean app:assembleRelease" // RELEASE APK 빌드
                bat "find $WORKSPACE -name '*.apk'"
            }
        }
    }
}